import 'package:flutter/material.dart';
import 'package:pintarr/content/setting/profile.dart';

class Setting extends StatelessWidget {
  const Setting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Profile();
  }
}
